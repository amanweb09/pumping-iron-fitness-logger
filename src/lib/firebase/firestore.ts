// import { database } from "./firebase.config"
// import { collection, query, where, getDocs } from "firebase/firestore";


// class FirestoreQuery {

//     read(c:string, condition?:string) {
//         // "capital", "==", true
//         let q;
//         if (condition) {
//             q = query(collection(database, c), where(condition));
//         }
//         else {
//             q = query(collection(database, c));
//         }
//     }

// }

// const fsQuery = new FirestoreQuery()
// export default fsQuery